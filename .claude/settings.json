{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "timeout": 60,
            "statusMessage": "Formatting with Biome…",
            "command": "FILE=$(jq -r '.tool_input.file_path // empty'); case \"$FILE\" in *.ts|*.tsx|*.js|*.jsx|*.mjs|*.cjs|*.json|*.jsonc|*.css) cd \"$CLAUDE_PROJECT_DIR\" && pnpm exec biome check --write \"$FILE\" 2>/dev/null || true ;; esac"
          }
        ]
      }
    ]
  }
}
